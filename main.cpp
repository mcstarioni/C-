#include "Interface.h"
#include "Manage.h"
#include <string>
#include <iostream>
#include <conio.h>
int main()
{
    system("color F0");
    Interface::mainMenu();
    /*
    State a;
    State b;
    State c;
    int n;
    std::cin>>n;
    a.setDefence(n);
    std::cout<<"comn def a = "<<*a.common_defence<<"\n";
    std::cin>>n;
    b.setDefence(n);
    std::cout<<"comn def b = "<<*b.common_defence<<"\n";
    std::cin>>n;
    c.setDefence(n);
    std::cout<<"comn def c = "<<*c.common_defence<<"\n";
    std::string name;
    std::cin>>name;
    Alliance nato(name);
    nato.joinState(&a);
        std::cout<<"comn def a = "<<*a.common_defence<<"\n";
    nato.joinState(&b);
        std::cout<<"comn def b = "<<*b.common_defence<<"\n";
    nato.joinState(&c);
        std::cout<<"comn def c = "<<*c.common_defence<<"\n";
    std::cout<<"Comn def = "<<nato.getDefence()<<"\n";
    getch();
    */
}
